# Invicta

A static HTML project served via `npx serve`.

## How to run

The workflow **"Start application"** serves the project on port 5000:

```
npx --yes serve . -l 5000
```

Open the **Preview** pane to see the result.

## Project structure

- `index.html` — main entry point

## User preferences

<!-- Add your preferences here -->
